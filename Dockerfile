# TODO use the from command to get an official Python runtime as a parent image
FROM 


#TODO copy the current directory into the container at /app
COPY 

# Run hello_world.py when the container launches
CMD 